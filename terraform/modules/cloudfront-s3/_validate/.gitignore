# Terraform working dir + lock file generated by `terraform init`.
# The _validate/ wrapper is for `terraform validate` only; lock files
# from a no-backend init aren't useful to commit.
.terraform/
.terraform.lock.hcl
